#include <stdio.h>

int main() {
	float f;
	printf("Nhap so bat ky: ", &f);
	scanf("%f", &f);
	int d;
	d = (int) f;
	
	if (d == f) 
		printf("d la so nguyen");
	else
		printf("d khong la so nguyen");
	return 0;
}
